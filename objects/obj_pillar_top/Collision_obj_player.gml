/// @description Insert description here
// You can write your code in this editor

other.x -= 7;

if(other.skill_harden_active && other.dash_act && other.image_xscale > 0.5){ 
	image_speed = 1
	} else {
		alarm[0] = 1;
	}
